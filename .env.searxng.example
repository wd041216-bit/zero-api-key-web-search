# Copy values from this file into your shell or a local .env before using docker-compose.searxng.yml.
# For production or long-lived setups, prefer pinning SEARXNG_IMAGE to a specific official tag.

SEARXNG_IMAGE=searxng/searxng:latest
SEARXNG_PORT=8080
ZERO_SEARCH_SEARXNG_URL=http://127.0.0.1:8080
