# Hermes MCP Server Configuration for zero_api_key_web_search
# Add this to your ~/.hermes/config.yaml under mcp_servers:

mcp_servers:
  zero_api_key_web_search:
    command: "zs-mcp"
    args: []
    enabled: true
    timeout: 120